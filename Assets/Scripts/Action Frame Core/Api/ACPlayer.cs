using Unity.Entities;

namespace ActionFrameCore
{
    public static class Player
    {
        //public static void RequestPlay(EntityCommandBuffer cmd, Entity reciever, Entity source, Entity action, ActionChannel channel)
        //{
        //    var b = cmd.get<ChannelEntry>(reciever);
        //    
        //}
    }
}